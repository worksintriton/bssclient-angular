import { ComponentRef } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { NavController } from '../../providers/nav-controller';
export declare class StackController {
    private stack;
    private containerEl;
    private router;
    private navCtrl;
    private viewsSnapshot;
    private views;
    constructor(stack: boolean, containerEl: HTMLIonRouterOutletElement, router: Router, navCtrl: NavController);
    createView(enteringRef: ComponentRef<any>, route: ActivatedRoute): RouteView;
    getExistingView(activatedRoute: ActivatedRoute): RouteView | null;
    canGoBack(deep: number): boolean;
    setActive(enteringView: RouteView, direction: number, animated: boolean): Promise<void>;
    pop(deep: number): void;
    private insertView(enteringView, direction);
    private cleanup();
    getActive(): RouteView | null;
    private transition(enteringView, leavingView, direction, animated, showGoBack);
    private getUrl(activatedRoute);
}
export declare function destroyView(view: RouteView): void;
export declare function getLastDeactivatedRef(views: RouteView[]): ComponentRef<any>;
export interface RouteView {
    url: string;
    element: HTMLElement;
    ref: ComponentRef<any>;
    deactivatedId: number;
}
